<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>CrisisHelper</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <% unless @hide_nav %>
      <div class="navbar navbar-fixed-top <%= "downstream" unless @hide_back_button %>">
        <% unless @hide_back_button %>
          <button type="button" class="back_button btn" onclick="history.go(-1)">
            <i class="icon-home"></i>
            Back
          </button>
        <% end %>

        <span class="title"><%= "#{@location.city}, #{@location.state}" %></span>
        <%# TODO %>
        <% unless @hide_menu %>
          <button type="button" class="btn">
            <i class="icon-align-justify"></i>
          </button>
        <% end %>
      </div>
    <% end %>

    <div class="container">

      <%= yield %>

    </div>

  </body>
</html>

