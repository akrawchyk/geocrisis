<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>CrisisHelper</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <% unless @hide_nav %>
      <% @nav_extra_klass ||= "" %>
      <div class="navbar navbar-fixed-top <%= @nav_extra_klass %>">
        <% if @show_back_button ||= false %>
          <button type="button" class="back_button btn" onclick="history.go(-1)">
            <i class="icon-home"></i>
            Back
          </button>
        <% end %>

        <span class="title"><%= @nav_title ||= 'Washington, VA' %></span>
        <button type="button" class="btn">
          <i class="icon-align-justify"></i>
        </button>
      </div>
    <% end %>

    <div class="container">

      <%= yield %>

    </div>

  </body>
</html>

